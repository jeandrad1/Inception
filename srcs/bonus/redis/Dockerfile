# Use the official Redis image from Docker Hub
FROM redis:latest

# The default command for the redis image is already `redis-server`,
# but explicitly stating it makes the Dockerfile clearer.
CMD ["redis-server"]